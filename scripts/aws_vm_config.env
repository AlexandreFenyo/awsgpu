# Configuration pour scripts/create-aws-vm.zsh
# Décommentez et ajustez les valeurs si nécessaire.

# Région AWS (par défaut eu-west-3)
AWS_REGION=eu-west-3

# Type d'instance (par défaut g4dn.xlarge)
INSTANCE_TYPE=g4dn.xlarge

# Nom de la key pair existante à utiliser (OBLIGATOIRE)
# Exemple :
# KEY_NAME=my-existing-key
KEY_NAME=fenyo-aws

# Optionnel : forcer une AMI spécifique (sinon le script recherche la dernière AMI correspondant au pattern)
# AMI_ID=ami-0123456789abcdef0

# Pattern utilisé pour trouver la dernière Deep Learning AMI PyTorch 2.7
# Laisser tel quel sauf si vous préférez un autre pattern
AMI_NAME_PATTERN="Deep Learning AMI*PyTorch 2.7*"

# Préfixe de nommage pour la VM et le security group
VM_NAME_PREFIX=awsgpu
SG_NAME_PREFIX=awsgpu-sg
